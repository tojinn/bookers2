<%= render 'homes/list' %>
<%= render 'books/info' %>
<div class="col-8 offset-md-1">
 <h2>Books</h2>
  <table class="table">
   <thead>
     <tr>
         <th>image</th>
         <th>name</th>
         <th></th>
     </tr>
   </thead>
   
   <tbody>
    <% @users.each do |users| %>
    <tr>
     <td><%= link_to user_path(@user.id) do %><%= attachment_image_tag @user, :profile_image,:fill, 60, 60, fallback: "no_image.jpg" %><% end %></td>
     <td><%= @user.name %></td>
     <td><%= link_to 'show', user_path(current_user.id) %></td>
    </tr>
   <% end %>
   </tbody>
  </table>
</div>
</div>
<tbody>
 <tr></tr>
</tbody>